{
  "name": "aprendizapp-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Aprendizapp backend",
  "main": "index.js",
  "scripts": {
    "clean": "npx lerna run clean",
    "debugger": "npx lerna run --scope api debugger --stream",
    "dev": "npx lerna run --scope api dev --stream",
    "build": "npx lerna run build",
    "init": "npx lerna bootstrap",
    "start": "npx lerna run start --stream",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^12.1.4",
    "@types/node": "^15.14.9",
    "@typescript-eslint/eslint-plugin": "^5.31.0",
    "@typescript-eslint/parser": "^5.31.0",
    "lerna": "^5.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "type": "module"
}
